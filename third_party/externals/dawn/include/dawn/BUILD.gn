# Stub BUILD.gn for wasm32-unknown-unknown builds.
# The real Dawn headers target provides webgpu.h — for WASM we vendor the
# header separately and pass it via -isystem, so this target is a no-op.

source_set("headers") {
  public_configs = []
}

source_set("cpp_headers") {
  public_configs = []
}
